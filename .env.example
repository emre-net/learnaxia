# --- DATABASE ---
# Railway PostgreSQL connection string (from Railway > PostgreSQL > Connect tab)
DATABASE_URL="postgresql://user:password@host:port/database"

# --- GOOGLE OAUTH ---
# Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# --- AUTH ---
# Production: Set to your Railway domain (e.g. https://learnaxia-production.up.railway.app)
# Development: http://localhost:3000
AUTH_URL="http://localhost:3000"

# Generate with: openssl rand -base64 32
AUTH_SECRET="generate-a-strong-random-secret"

# --- MOBILE JWT ---
# Secret for /api/mobile/login access tokens (Authorization: Bearer ...)
MOBILE_JWT_SECRET="generate-a-strong-random-secret"

# --- MOBILE REFRESH TOKENS ---
# Pepper used only for hashing refresh tokens at rest (server-side only).
MOBILE_REFRESH_TOKEN_PEPPER="generate-a-strong-random-secret"
